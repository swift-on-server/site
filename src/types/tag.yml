id: tag

properties:

context:
    site:
        tags:
            sort: title
            order: asc
            pagination:
                limit: 10
                
    local:
        posts:
            references: post
            foreignKey: tags
            sort: title
            order: asc
            limit: 10

template: blog.single.tag
