{{<html}}
{{$main}}

<div id="tag">
    
    <header>
        {{#page.image}}<img class="medium" src="{{page.image}}" alt="{{page.title}}">{{/page.image}}
        <h1>{{page.title}}</h1>
        <p>{{page.description}}</p>
        
    </header>

    {{& page.contents.html}}
    
    <p>{{count(page.posts)}} posts</p>
    
    {{#empty(page.posts)}}
    Empty.
    {{/empty(page.posts)}}
    {{^empty(page.posts)}}
    <div class="grid grid-321">
    {{#page.posts}}
        {{> partials.blog.post}}
    {{/page.posts}}
    </div>
    {{/empty(page.posts)}}
</div>

{{/main}}
{{/html}}
